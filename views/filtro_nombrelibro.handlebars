<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Filtro por Título de Libro</title>
</head>
<body>
    <h1>Filtrar Libros por Título</h1>

   
    <form action="/filtro_nombrelibro" method="POST">
        <label for="titulo">Título del Libro:</label>
        <input type="text" id="titulo" name="titulo" value="{{titulo}}" required>
        <button type="submit">Filtrar</button>
    </form>

   
    {{#if libros}}
        <h2>Resultados para el Título: "{{titulo}}"</h2>
        
        {{#if libros.length}}
            <table border="1">
                <thead>
                    <tr>
                        <th>ID Libro</th>
                        <th>Título</th>
                        <th>Autor</th>
                        <th>Editorial</th>
                        <th>Páginas</th>
                        <th>Genero</th>
                        <th>Cantidad</th>
                    </tr>
                </thead>
                <tbody>
                    {{#each libros}}
                        <tr>
                            <td>{{this.id_libro}}</td>
                            <td>{{this.titulo}}</td>
                            <td>{{this.autor_nombre}}</td>
                            <td>{{this.editorial_nombre}}</td>
                            <td>{{this.paginas}}</td>
                            <td>{{this.nombre_genero}}</td>
                            <td>{{this.cantidad}}</td>
                        </tr>
                    {{/each}}
                </tbody>
            </table>
        {{else}}
            <p>No se encontraron libros con el título "{{titulo}}".</p>
        {{/if}}
    {{/if}}
</body>
</html>
